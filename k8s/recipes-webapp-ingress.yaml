apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recipes-webapp
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: recipes-webapp
            port:
              number: 80